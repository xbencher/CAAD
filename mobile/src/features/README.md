# features

Each product feature (discovery, requests, chat, etc.) gets its own
directory here, bundling that feature's screens, components, hooks and
API calls together.

Empty in CP-0 — product features start in later checkpoints.
