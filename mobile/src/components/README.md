# components

Shared, presentational UI components with no feature-specific business
logic (buttons, cards, inputs, etc.). Feature-specific components live
under `src/features/<feature>/` instead.

Empty in CP-0 — no product UI has been built yet.
